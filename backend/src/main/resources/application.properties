spring.application.name=TnP_Connect

# ===================================================================
# LOCAL DEVELOPMENT — PostgreSQL
# ===================================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/tnp_Connect_platform
spring.datasource.username=postgres
spring.datasource.password=Ayush@0827

# ===================================================================
# JPA / Hibernate
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===================================================================
# File Upload
# ===================================================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads

# ===================================================================
# Brevo HTTP API
# ===================================================================
brevo.api.key=REPLACE_WITH_YOUR_BREVO_API_KEY
brevo.sender.email=rajrai092004@gmail.com
brevo.sender.name=TnP Connect

# ===================================================================
# Gemini AI
# ===================================================================
ai.gemini.api-key=AIzaSyBcQlHyayKnjerhE-PJY8YQlixU01ATqsI
ai.gemini.model=gemini-2.5-flash

# ===================================================================
# JWT
# ===================================================================
jwt.secret=tnpConnectSuperSecretJwtKeyForLocalDev2026

# ===================================================================
# Cloudinary (File Storage — works on both local & Render)
# ===================================================================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:your-cloud-name}
cloudinary.api-key=${CLOUDINARY_API_KEY:your-api-key}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:your-api-secret}